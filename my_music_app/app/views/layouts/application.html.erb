<!DOCTYPE html>
<html>
  <head>
    <title>MyMusicApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <header>
      <center><h1 style="color: red; border: 1px solid black;">My super music app!</h1></center>
      <% if logged_in? %>
      <form action="<%= session_url %>" method="POST">
        <input type="hidden" name="_method" value="DELETE">
        <input type="hidden" name="where_is_my_auth_token?" value="form_authenticity_token">

        <input type="submit" value="ByeBye">
      </form>

      <% else %>
        <a href="<%= new_user_url %>">Sign Up</a>
        <a href="<%= new_session_url %>">Login</a>
      <% end %>

    </header>
    <%= yield %>
  </body>
</html>
